class Vcircle {

  //float xpos, ypos;
  //float radius = 100;

  //int fadeInTimer = 500;
  //int breathTime = 500;
  //TimeLine fadeInTimer = new TimeLine(fateInTime);
  //TimeLine breathTimer = new TimeLine(breathTime);


  //color colorOfCircle = color(0, 177, 106); //green

  ////constructor
  //Vcircle(float x, float y) {
  //  xpos = x;
  //  ypos = y;
  //  fadeInTimer.startTimer();
  //}


  ////functions
  //display() {
  //  if(fadeInTimer.liner() < 1) {
  //    fade(true, fadeInTimer.liner(), colorOfCircle, )
  //  }

  //}

  ////update() { }



  //void fade(boolean fadeIn, float rate, color c, float transparent) {
  //  //compress the signal
  //  if(rate > 1)
  //    rate = 1;

  //  if(fadeIn)
  //    transparent = rate* transparent;
  //  else
  //    transparent = transparent * (1 - rate);

  //  noStroke();
  //  fill(c, transparent);
  //  ellipse(xpos, ypos, 2*radius, 2*radius);
  //}

}